In OpenChamber there's a thing for "inject this at the start of each new session".

Communications:

Please don't use em-dashes or semicolons in text included for consumption by others.  I prefer more natural sounding responses.  It doesn't even have to be complete sentences for the purposes of slack messaging, etc.


Context preserving:

Use subagents whenever possible to conserve your context window. Act as the orchestrator, not the implementer.


Code reviews (for work they did):

Run an adversarial code review in a sub-agent and address the findings.