Identify locally running instance and port (default: 5432):
pg_isready

Connect with psql CLI (default maintenance db name: postgres)
psql postgres

psql Command to see list of roles (after connecting with CLI)
\du

local idp defaults:
- server: localhost
- maintenance db: postgres
- username: <mac username>