Yeah you have to create an account

Then you can use search to see if there is an active profile

Then use the proof_user endpoint submit the info for the process

Then if the user passes it will send them a confirmation email

Will take them to the confirmation page